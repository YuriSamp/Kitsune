---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="w-28">
	{
		date.toLocaleDateString('pt-br', {
			year: 'numeric',
			month: '2-digit',
			day: "2-digit",
		})
	}
</time>
